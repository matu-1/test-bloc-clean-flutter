import 'package:flutter/material.dart';

class AnimeCreatePage extends StatelessWidget {
  static final routeName = 'animecreate';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nuevo anime'),
      ),
    );
  }
}
